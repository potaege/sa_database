generator client {
  provider = "prisma-client-js"
}

datasource easy_service {
  provider     = "postgresql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Users {
  id       Int      @id @default(autoincrement())
  username String   @easy_service.VarChar(20)
  password String   @easy_service.VarChar(70)
  salt     String   @easy_service.VarChar(16)
  name     String   @easy_service.VarChar(20)
  surname  String   @easy_service.VarChar(20)
  address  String?  @easy_service.VarChar(50)
  province String   @easy_service.VarChar(20)
  role     String   @easy_service.VarChar(20)
  add_date DateTime @default(now())

  works                 Works[]
  movedItems            Transaction_logs[]      @relation("UserLog")
  receivedItems         Transaction_logs[]      @relation("FromUserLog")
  Spare_parts_engineers Spare_parts_engineers[]
}

model Spare_parts {
  id          Int      @id @default(autoincrement())
  name        String   @unique() @easy_service.VarChar(40)
  description String?  @easy_service.VarChar(100)
  unit        String   @easy_service.VarChar(15)
  price       Float
  add_date    DateTime @default(now())

  Spare_parts_requests  Spare_parts_requests[]
  Transaction_logs      Transaction_logs[]
  Spare_parts_engineers Spare_parts_engineers[]
}

model Customers {
  id           Int      @id @default(autoincrement())
  name         String   @easy_service.VarChar(30)
  credit_limit Float
  address      String?  @easy_service.VarChar(50)
  tax_id       String?  @easy_service.VarChar(15)
  tel          String   @easy_service.VarChar(12)
  province     String   @easy_service.VarChar(20)
  add_date     DateTime @default(now())

  Works Works[]
}

model Works {
  id           Int       @id @default(autoincrement())
  userID       Int?
  customerID   Int
  mail_date    DateTime  @default(now())
  service_date DateTime? @easy_service.Date
  status       Int       @default(0)
  address      String    @easy_service.VarChar(50)
  province     String    @easy_service.VarChar(20)
  add_date     DateTime  @default(now())

  user_id         Users?            @relation(fields: [userID], references: [id])
  customer_id     Customers         @relation(fields: [customerID], references: [id])
  Requests        Requests[]
  AdditionalCosts AdditionalCosts[]

  @@index([customerID])
  @@index([userID])
}

model AdditionalCosts {
  id          Int      @id @default(autoincrement())
  workID      Int
  description String   @easy_service.VarChar(100)
  cost        Float
  amount      Int
  unit        String   @easy_service.VarChar(10)
  add_date    DateTime @default(now())

  work_id Works @relation(fields: [workID], references: [id])

  @@index([workID])
}

model Requests {
  id          Int      @id @default(autoincrement())
  model       String   @easy_service.VarChar(20)
  sn          String   @easy_service.VarChar(20)
  rated       String?  @easy_service.VarChar(10)
  description String?  @easy_service.VarChar(100)
  warranty    Boolean
  workID      Int
  add_date    DateTime @default(now())

  work_id              Works                  @relation(fields: [workID], references: [id], onDelete: Cascade)
  Spare_parts_requests Spare_parts_requests[]

  @@index([workID])
}

model Spare_parts_requests {
  id              Int      @id @default(autoincrement())
  sparePartID     Int
  requestID       Int
  spare_parts_qty Int
  description     String?  @easy_service.VarChar(100)
  sn              String?  @easy_service.VarChar(20)
  add_date        DateTime @default(now())

  spare_parts_id Spare_parts @relation(fields: [sparePartID], references: [id])
  request_id     Requests    @relation(fields: [requestID], references: [id])

  @@index([sparePartID])
  @@index([requestID])
}

model Transaction_logs {
  id          Int      @id @default(autoincrement())
  sparePartID Int
  userID      Int
  fromUserID  Int
  quantity    Int
  status      Int      @default(0)
  add_date    DateTime @default(now())

  spare_part_id Spare_parts @relation(fields: [sparePartID], references: [id])
  user_id       Users       @relation("UserLog", fields: [userID], references: [id])
  rom_user_idf  Users       @relation("FromUserLog", fields: [fromUserID], references: [id])

  @@index([sparePartID])
  @@index([userID])
  @@index([fromUserID])
}

model Spare_parts_engineers {
  id            Int      @id @default(autoincrement())
  spare_part_id Int
  user_id       Int
  quantity      Int
  add_date      DateTime @default(now())

  user       Users       @relation(fields: [user_id], references: [id])
  spare_part Spare_parts @relation(fields: [spare_part_id], references: [id])

  @@index([user_id])
  @@index([spare_part_id])
}
